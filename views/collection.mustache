<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Jackets - Collection</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/css/materialize.min.css">
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <div class="container">
      <h1 class="center-align">Jackets</h1>
      {{#model}}
        <div class="padding margin blue lighten-4">
          <div class="">
            Style: {{style}}
          </div>
          <div class="">
            Brand: {{brand}}
          </div>
          <div class="">
            Color: {{#color}}{{.}}{{/color}}
          </div>
          <div class="">
            Materials:
            <ul>
              {{#materials}}
                <li>{{percentage}}% {{fabricType}}</li>
              {{/materials}}
            </ul>
          </div>
          <div class="flex">
            <form class="" action="/delete" method="post">
              <input type="hidden" name="id" value="{{_id}}">
              <button class="btn margin red" type="submit" name="button">Delete</button>
            </form>
            <form class="" action="/edit" method="post">
              <input type="hidden" name="id" value="{{_id}}">
              <button class="btn margin yellow darken-1" type="submit" name="button">Edit</button>
            </form>
          </div>

        </div>
      {{/model}}

    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/js/materialize.min.js"></script>
  </body>
</html>
